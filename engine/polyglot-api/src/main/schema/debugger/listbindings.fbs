include "common.fbs";
namespace org.enso.polyglot.debugger.protocol;

table Binding {
  name: string (key);
  value: ObjectRepr;
}

table ListBindingsRequest {}

table ListBindingsResult {
  bindings: [Binding];
}